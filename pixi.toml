[project]
name = "1D-MCSolver"
version = "0.1.0"
description = "Add a short description here"
authors = ["Nathan Glaser <nglaser3@illinois.edu>"]
channels = ["conda-forge"]
platforms = ["osx-arm64", "linux-64"]

[tasks]
compile =  "mkdir build; cd build; cmake ..; cmake --build .; cd ../"

[dependencies]
pugixml = ">=1.14,<2"
cmake = ">=3.30.1,<4"
